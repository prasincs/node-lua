h2 So you think you can write Lua?
#container
  #code
    textarea(id='luaCodeBox',rows='30', cols='80')
      | function double(x)
      |  return 2*x
      | end
      |  
      | x = 10
      | print ("Double of ".. x .. " is ".. double(x))
    button(id='runLua', type='button') Run
  pre#output
